services:
  workout-tracker:
    pull_policy: build
    build:
      context: .
      dockerfile: Dockerfile
    ports:
      - "8000:8000"
    volumes:
      - workout_tracker_data:/app/app/data
    restart: unless-stopped

volumes:
  workout_tracker_data:
